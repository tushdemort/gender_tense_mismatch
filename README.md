Run the run.sh

It will run the new_gender_tense which takes the doc_sheet as input and gets the gender and tense score, an intermediate file gender_tense_output.csv is created

It then runs word order file which saves the final output as final.csv
